<Page
    x:Class="DailyDiary.View.Write.NewDiary"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DailyDiary.View.Write"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="TextBox" Grid.Row="0" FontSize="25" TextWrapping="Wrap" AcceptsReturn="True" MaxHeight="700" Width="550" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="0,0,2,0"/>
        <RelativePanel MinHeight="300" Grid.Row="1">
            <Button x:Name="InsertTodoBtn" Grid.Row="1" Background="#FFD101" Click="InsertTodoBtn_Click" Content="Save" RelativePanel.AlignRightWithPanel="True" Margin="0,10,0,-10"/>
        </RelativePanel>
    </Grid>
</Page>
